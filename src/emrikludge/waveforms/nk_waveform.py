def generate_nk_waveform(trajectory: GeodesicTrajectory,
                         params: EMRIParameters,
                         config: WaveformConfig) -> Tuple[np.ndarray, np.ndarray]:
    """
    输出 h_plus(t), h_cross(t)
    """
    ...
